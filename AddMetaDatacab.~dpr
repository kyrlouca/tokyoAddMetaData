program AddMetaDataCab;

uses
  Forms,
  temp in 'temp.pas' {tempFRM},
  MainE in 'MainE.pas' {MainEFRM};

{$R *.RES}

begin
  Application.Initialize;
  Application.CreateForm(TMainEFRM, MainEFRM);
  Application.Run;
end.
